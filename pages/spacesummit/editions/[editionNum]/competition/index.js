import React from "react";
import { useRouter } from "next/router";

const Competition = () => {
  // const {
  //   query: { editionNum },
  // } = useRouter();
  // const { form } = editions.find((elem) => elem.id == editionNum);

  return <main className="__root"></main>;
};

export default Competition;
