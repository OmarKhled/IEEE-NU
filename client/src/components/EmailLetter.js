import React, { useState } from 'react'
import { Modal } from "react-bootstrap"

const EmailLetter = () => {
  return (
    <div>
      {/* <button className="btn btn-danger" onClick={() => setModalShow(true)}>Modal</button> */}
    </div>
  )
}

export default EmailLetter
